# Planner Skills Spec

## Purpose
Test fixture for spec slicing.

## Invariants (Non-Negotiable Rules)
Test-only invariants.

## Column Resolution Rules
Test-only column rules.

## Output JSON Contract (High-Level)
Test-only contract.

### Operation: add_columns
Add columns spec for tests.

### Operation: filter_rows (op: filter)
Filter rows spec for tests.

### Operation: rename_columns (op: rename)
Rename columns spec for tests.
